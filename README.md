# Podgon Ceny - Автоматизация обновления цен на Wildberries

Автоматизированная система для обновления цен товаров на Wildberries с использованием рекомендуемых цен из Excel шаблона.

## Возможности

- 📥 **Автоматическое скачивание Excel шаблона** с рекомендуемыми ценами через Selenium
- ✏️ **Корректировка цен** - автоматически устанавливает цену на 1 рубль меньше рекомендуемой
- 📤 **Загрузка цен на WB** - обновление цен через API Wildberries
- 🔄 **Автоматизация** - готово к запуску по расписанию (cron)
- 🖥️ **Кроссплатформенность** - работает на Windows и Linux (Ubuntu VPS)

## Установка

### Требования

- Python 3.8+
- Chrome/Chromium браузер (для скачивания шаблона)
- Selenium WebDriver
- API токен Wildberries

### Установка зависимостей

**Рекомендуется использовать виртуальное окружение:**

```bash
# Windows
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Или установите вручную:
pip install selenium openpyxl requests python-dotenv
```

### Настройка

1. Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Настройте `.env` файл:
```env
WB_API_TOKEN=ваш_api_токен_wildberries
HEADLESS_BROWSER=false  # true для Linux серверов
TARGET_DIR=./  # Директория для работы (на сервере: ~/wildberries/price)
BASE_DIR=./    # Базовая директория (опционально)
```

3. Первая авторизация:
   - Запустите скрипт с `HEADLESS_BROWSER=false`
   - Авторизуйтесь вручную через SMS (требуется только один раз)
   - Cookies сохранятся автоматически

## Использование

### Основной скрипт (все в одном)

Запускает весь процесс автоматически:

```bash
python update_wb_prices_from_template.py
```

Скрипт выполняет:
1. Скачивание актуального Excel шаблона
2. Корректировку цен (J = N - 1)
3. Загрузку скорректированных цен на WB через API

### Отдельные модули

#### Скачивание шаблона

```bash
python test_download_excel.py
```

#### Корректировка цен в Excel

```bash
python update_prices.py [путь_к_файлу.xlsx]
```

Если путь не указан, автоматически найдет последний скачанный шаблон.

#### Обновление цен и остатков через API

```bash
python update_wb_stocks_prices.py
```

## Структура проекта

- `update_wb_prices_from_template.py` - основной скрипт (объединяет все функции)
- `test_download_excel.py` - автоматическое скачивание Excel шаблона
- `update_prices.py` - корректировка цен в Excel файлах
- `update_wb_stocks_prices.py` - обновление остатков и цен через API WB

## Документация

- `README_AUTO_DOWNLOAD.md` - подробная документация по автоматическому скачиванию
- `VPS_DEPLOYMENT.md` - инструкция по развертыванию на Ubuntu VPS
- `README_VPS.md` - краткая инструкция для VPS
- `IMPROVEMENTS_AUTH.md` - описание улучшений авторизации

## Автоматизация

### Windows (Планировщик задач)

Создайте задачу в Планировщике задач Windows для запуска скрипта по расписанию.

### Linux (Cron)

```bash
# Редактируйте crontab
crontab -e

# Запуск каждый день в 3:00
0 3 * * * cd /path/to/project && /usr/bin/python3 update_wb_prices_from_template.py >> logs/update.log 2>&1
```

## Развертывание на VPS

См. подробную инструкцию в `VPS_DEPLOYMENT.md`

Краткая версия:
1. Установите зависимости: `sudo bash install_vps.sh`
2. Скопируйте проект и cookies
3. Настройте `.env` с `HEADLESS_BROWSER=true`
4. Запустите скрипт

## Безопасность

⚠️ **Важно**: 
- Не коммитьте файл `.env` в git
- Не коммитьте файлы cookies (`wb_cookies.pkl`)
- Используйте `.gitignore` для исключения чувствительных данных

## Лицензия

MIT

## Автор

SpiritWalker84

